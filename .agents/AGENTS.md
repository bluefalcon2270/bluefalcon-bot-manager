# Project Rules

- Always bump the version by at least 0.1 in the script file whenever making a change.
- Always append the changes and bump the version accordingly in the `CHANGELOG.md` file.
- ALWAYS wait for user approval before making changes. Provide a preview of what the changes will look like. Only execute the changes when the user explicitly agrees or says "ok do it".
